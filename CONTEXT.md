# CONTEXT — Geo-Knowledge Q&A 领域术语表 v2

> 最后更新：2026-07-05
> 此文件为纯术语定义，不含实现细节。实现细节见 `docs/` 目录。

---

## 核心领域概念

### Chunk（文本切片）

从 PDF 文档中切出的文本片段，是 RAG 检索的最小单元。每个 chunk 携带来源页码、文档标题、分词结果和 TF-IDF 稀疏向量。

- **窗口大小**：512 字符（浮动范围 50-560），128 字符重叠
- **切分策略**：优先在段落边界（空行）和句号处断句，保证语义完整
- **存储位置**：SQLite `chunks` 表（含向量 BLOB）

### LandUse（用地分类节点）

从《用地用海分类指南》中提取的国土空间用地分类条目。按编码长度分为三级：

- **大类**：2 位编码（如 `01 耕地`、`02 园地`）—— 共 24 个
- **中类**：4 位编码（如 `0101 水田`、`0201 果园`）—— 共 125 个
- **关系**：中类 `BELONGS_TO`（属于）大类，按编码前缀推断

### Concept（规划概念节点）

从《市级国土空间总体规划编制指南》中通过 LLM 提取的规划领域概念。

- **类型限制**：空间管控 | 规划指标 | 功能分区 | 政策要求 | 实体
- **关系类型**：BELONGS_TO（属于）| GOVERNS（管控）| CONSTRAINS（约束）| REFERENCES（引用）
- **提取方式**：分段文本 → DeepSeek API → 结构化 JSON → 去重合并 → Neo4j

### KG Path（知识图谱路径）

两个实体或概念之间的语义关系链，以三元组形式表示：`(实体A) -[关系]-> (实体B)`。例如：

```
养老院 -[属于]-> 社会福利用地 -[属于]-> 公共管理与公共服务用地
```

前端展示为关系链，后端通过 Neo4j Cypher 多跳查询获取。

### 双路检索（Dual-Path Retrieval）

问答系统的核心检索策略，两条路径并行执行：

- **RAG 路径**：用户问题 → jieba 分词 → TF-IDF 向量化 → SQLite 余弦相似度 Top-K chunks
- **KG 路径**：用户问题 → 关键词提取 → Neo4j 实体匹配 → Cypher 多跳查询 → 关系链
- **合并**：两路上下文拼接后送入 LLM 生成最终回答

---

## 系统概念

### Document（文档）

系统中的一份知识源文件。当前包含两份 PDF：

| 文档 | 用途 |
|------|------|
| 市级国土空间总体规划编制指南（2020年）| RAG 检索主文档（46 页）|
| 用地用海分类指南 | KG 实体提取主文档（35 页）|

### Conversation（对话）

一次问答会话。包含多条 Message，由前端管理生命周期。当前版本不持久化到后端。

### Message（消息）

对话中的一条消息，角色为 `user` 或 `assistant`。assistant 消息可携带 `sources`（来源引用）和 `kgContext`（KG 关系链）。

### Source（引用来源）

回答中引用的文档片段。包含 `docId`、`docTitle`、`page`、`snippet`。

### Subgraph（子图）

Neo4j 中某一知识领域的局部图结构。前端 GraphView 通过 `GET /api/kg/subgraph?keyword=` 获取，用于可视化展示。包含 `nodes[]` 和 `edges[]`。

---

## 待定

- [ ] 空间查询的领域概念（终期加入 PostGIS 后补充）
- [ ] Agent 检索编排（多轮对话场景，终期考虑）
- [ ] BERT 语义检索升级路径（终期考察）
