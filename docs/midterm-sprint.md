# 中期答辩冲刺计划

> 答辩日期：2026-07-06（后天）
> 当前状态：三层代码各自完成，待集成联调

---

## 验收标准

- [x] 前端 Q&A 页面输入问题 → 后端双路检索 → LLM 生成回答 → 前端展示答案 + 来源 + KG 关系链
- [x] 前端 GraphView 调用 `/api/kg/subgraph` 展示真实 Neo4j 数据（力导向图）
- [x] Neo4j Browser 可独立展示知识图谱（答辩备用方案）
- [x] Neo4j dump 文件可分发（200KB，队友导入即用）
- [ ] 全员端到端联调：四进程并行启动，逐一走通 QA + GraphView + AdminView
- [ ] 准备 5 个测试问题 + 录屏 3-4 分钟演示视频
- [ ] 答辩 PPT（按 `docs/midterm-slides-outline.md` 大纲制作）

---

## 集成问题清单

| # | 问题 | 状态 |
|---|------|------|
| 1 | 后端连 Phase 1 SQLite | ✅ Flask 桥接 `ml-service/server.py` |
| 2 | TF-IDF 对接 pkl 模型 | ✅ 统一模型 2649 词，实时向量化 |
| 3 | GraphView 接真实数据 | ✅ A 同学重写力导向图，接 `/api/kg/subgraph` |
| 4 | 后端环境变量 | ✅ `.env` 已配 DeepSeek key + Neo4j 密码 |
| 5 | 端到端联调 | ⬜ 四进程同时跑一遍 |
| 6 | 测试问题 + 录屏 | ⬜ 待准备 |
| 7 | 答辩 PPT | ⬜ 大纲已写，待做 PPT |
| 8 | LLM 不可用 → mock | ✅ 已有降级 |
| 9 | Neo4j 不可用 → 空返回 | ✅ 已有降级 |

### 额外修复（2026-07-05）

| 修复项 | 状态 |
|--------|------|
| LLM 回答格式纯段落化 + 前端 kgContext 标签展示 | ✅ |
| 闲聊阈值拦截（TF-IDF top-1 < 0.04） | ✅ |
| 概念名语义过滤（数字编码/句首片段/名称粘连） | ✅ |
| LandUse 名称清洗（去编码碎片，149→137 节点） | ✅ |
| QaView 错误提示 + 标题更新 + localStorage 持久化 + 加载骨架屏 | ✅ |

---

## 剩余排期（7/6 上午）

| 时间 | 任务 | 人员 |
|------|------|------|
| 08:00-08:30 | 全员四进程联调：确认 QA + GraphView + Admin 正常 | 全员 |
| 08:30-09:30 | 录屏 3-4 分钟 + 准备 5 个测试问题 | 全员 |
| 09:30-10:00 | 做 PPT（按 `docs/midterm-slides-outline.md`） | 讲的人 |
| 10:00-10:30 | 全流程演练 × 1 次 | 全员 |

---

## 降级方案

| 风险 | 备用方案 |
|------|---------|
| TF-IDF 桥接来不及 | 继续用 B 自写的 JS 版 TF-IDF + 种子数据，答辩展示检索逻辑可用 |
| LLM API 不可用 | 后端自动回退 mock 模式，展示 RAG 检索结果 + 来源标注 |
| Neo4j 挂了 | 后端 KG 查询返回空，前端展示纯 RAG 回答 |
| GraphView 来不及接 | 用 Neo4j Browser 直接展示图（已经能看），前端展示静态 demo |
| 端到端联调不通 | 分开演示：Neo4j Browser + 前端 mock 检索 + 命令行 Python 预处理 |
| 全部卡住 | PPT 展示架构 + Neo4j 截图 + 数据截图 |

---

## 预备测试问题（建议）

1. **RAG-only**："城镇开发边界的主要管控要求是什么？"
2. **KG-assisted**："社会福利用地属于哪个大类？它的上一级分类是什么？"
3. **混合**："养老院应该划入什么用地类型？相关管控要求有哪些？"
4. **混合**："生态保护红线与永久基本农田之间有什么关系？"
5. **边界测试**："如何处理城市开发边界内的存量建设用地？"
